export enum CommandTypes {
  // Command with a single answer
  SIMPLE = 'simple',
  // Command that have many possible answers
  VARIANT = 'variant',
  // Command that have a chain of answers
  CHAIN = 'chain',
}
